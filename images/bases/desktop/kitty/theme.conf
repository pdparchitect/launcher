# Terminal palette for the desktop base.
#
# Intentionally empty: the base leaves kitty on its built-in dark palette. A
# product image replaces this file through its overlay to theme the terminal,
# which is why the generated kitty.conf includes it.
