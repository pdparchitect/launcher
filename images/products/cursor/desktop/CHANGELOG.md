# Changelog

Cursor Agent Desktop versions independently of the substrate. Its published
image version includes both versions because the image embeds both, and the
substrate version is also recorded on the image itself.

## [0.1.0]

- Pin Cursor Agent CLI build 2026.07.23-e383d2b for AMD64 and ARM64, with
  checksummed official terminal packages and no Cursor editor installation.
- Preserve Cursor Agent authentication, configuration, rules, skills,
  sessions, and project state below `/home/agent/.cursor` across restarts.
- Apply Cursor's `#1B1913` terminal canvas, `#14120B` desktop chrome, official
  off-white, and 2D cube identity across the browser landing page, wallpaper,
  desktop toolbar, window decorations, and Launcher artwork.
