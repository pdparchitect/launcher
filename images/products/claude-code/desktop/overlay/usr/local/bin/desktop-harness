#!/bin/bash
# desktop-harness - Control-Shift-G opens another Claude Code session.

set -euo pipefail

exec kitty \
    --title "${DESKTOP_TITLE:-Claude Code}" \
    -e bash -lc 'cd /workspace && exec claude'
