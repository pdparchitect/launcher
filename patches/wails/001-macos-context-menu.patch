diff --git a/internal/frontend/desktop/darwin/WailsContext.m b/internal/frontend/desktop/darwin/WailsContext.m
index 642c731..f3e49bf 100644
--- a/internal/frontend/desktop/darwin/WailsContext.m
+++ b/internal/frontend/desktop/darwin/WailsContext.m
@@ -280,6 +280,10 @@ extern void didReceiveNotificationResponse(const char *jsonPayload, const char*
     config.userContentController = userContentController;
     self.userContentController = userContentController;
 
+    // Let the page use the clipboard without WebKit's native paste prompt
+    [config.preferences setValue:@YES forKey:@"javaScriptCanAccessClipboard"];
+    [config.preferences setValue:@YES forKey:@"DOMPasteAllowed"];
+
     if (self.devtoolsEnabled) {
         [config.preferences setValue:@YES forKey:@"developerExtrasEnabled"];
     }
@@ -287,7 +291,7 @@ extern void didReceiveNotificationResponse(const char *jsonPayload, const char*
     if (!self.defaultContextMenuEnabled) {
         // Disable default context menus
         WKUserScript *initScript = [WKUserScript new];
-        [initScript initWithSource:@"window.wails.flags.disableDefaultContextMenu = true;"
+        [initScript initWithSource:@"if (window.wails && window.wails.flags) { window.wails.flags.disableDefaultContextMenu = true; }"
                      injectionTime:WKUserScriptInjectionTimeAtDocumentEnd
                   forMainFrameOnly:false];
         [userContentController addUserScript:initScript];
@@ -299,6 +303,7 @@ extern void didReceiveNotificationResponse(const char *jsonPayload, const char*
 
     CGRect init = { 0,0,0,0 };
     [self.webview initWithFrame:init configuration:config];
+    self.webview.defaultContextMenuEnabled = self.defaultContextMenuEnabled;
     [contentView addSubview:self.webview];
     [self.webview setAutoresizingMask: NSViewWidthSizable|NSViewHeightSizable];
     CGRect contentViewBounds = [contentView bounds];
diff --git a/internal/frontend/desktop/darwin/WailsWebView.h b/internal/frontend/desktop/darwin/WailsWebView.h
index b6f746c..7234bce 100644
--- a/internal/frontend/desktop/darwin/WailsWebView.h
+++ b/internal/frontend/desktop/darwin/WailsWebView.h
@@ -9,6 +9,7 @@
 @interface WailsWebView : WKWebView
 @property bool disableWebViewDragAndDrop;
 @property bool enableDragAndDrop;
+@property bool defaultContextMenuEnabled;
 @end
 
 #endif /* WailsWebView_h */
diff --git a/internal/frontend/desktop/darwin/WailsWebView.m b/internal/frontend/desktop/darwin/WailsWebView.m
index de23ac7..03d41b0 100644
--- a/internal/frontend/desktop/darwin/WailsWebView.m
+++ b/internal/frontend/desktop/darwin/WailsWebView.m
@@ -5,6 +5,16 @@
 @implementation WailsWebView
 @synthesize disableWebViewDragAndDrop;
 @synthesize enableDragAndDrop;
+@synthesize defaultContextMenuEnabled;
+
+- (NSMenu *)menuForEvent:(NSEvent *)event
+{
+  if ( !defaultContextMenuEnabled ) {
+    return nil;
+  }
+
+  return [super menuForEvent:event];
+}
 
 - (BOOL)prepareForDragOperation:(id<NSDraggingInfo>)sender
 {
